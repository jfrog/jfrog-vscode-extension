[tool.poetry]
name = "legacy-poetry"
version = "0.1.0"

[tool.poetry.dependencies]
python = "^3.10"
# A comment line
fire = "0.1.3"
newrelic = { version = "2.0.0.1", optional = true }

[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
