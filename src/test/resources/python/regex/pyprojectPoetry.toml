[tool.poetry]
name = "legacy-poetry"
version = "0.1.0"

[tool.poetry.dependencies]
python = "^3.10"
# A comment line that mentions "requests"
fire = "0.1.3"
newrelic = { version = "==2.0.0.1", optional = true }
"zope.interface" = "^6.0"
numpy = "1.26.*"

[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
